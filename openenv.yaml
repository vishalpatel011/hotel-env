spec_version: 1
name: hotel-booking-env
type: space
runtime: fastapi
app: server.app:app

tasks:
  - id: easy
    name: easy
    enabled: true
    description: "Basic task"
    grader:
      type: llm
      prompt_template: "Return a score between 0.01 and 0.99"

  - id: medium
    name: medium
    enabled: true
    description: "Intermediate task"
    grader:
      type: llm
      prompt_template: "Return a score between 0.01 and 0.99"

  - id: hard
    name: hard
    enabled: true
    description: "Advanced task"
    grader:
      type: llm
      prompt_template: "Return a score between 0.01 and 0.99"